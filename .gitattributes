# Force LF line endings for shell scripts that may be executed in Linux
# environments. Without this, Windows git clients check out these files
# with CRLF, which breaks bash ("$'\r': command not found").
*.sh text eol=lf
