# PDF OCR + Compression — Python dependencies
# Note: Tesseract OCR and Ghostscript are external system tools (installed outside pip)
# Authoritative source is pyproject.toml; this file mirrors it for `pip install -r` users.

# Core PDF processing
ocrmypdf>=16.0.0
pikepdf>=9.0.0
pdfminer.six>=20231228

# CLI + terminal output
typer>=0.12.0
rich>=13.7.0

# Web GUI
streamlit>=1.36.0

# REST API
fastapi>=0.110.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
